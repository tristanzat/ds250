---
title: "Client Report - Exploring Names"
subtitle: "Unit 1 Task 1"
author: "Tristan Zatylny"
format:
  html:
    self-contained: true
    page-layout: full
    title-block-banner: true
    toc: true
    toc-depth: 3
    toc-location: body
    number-sections: false
    html-math-method: katex
    code-fold: true
    code-summary: "Show the code"
    code-overflow: wrap
    code-copy: hover
    code-tools:
        source: false
        toggle: true
        caption: See code
execute: 
  warning: false
    
---

```{python}
import polars as pl
import numpy as np
from lets_plot import *
from polars import col

LetsPlot.setup_html(isolated_frame=True)
```

```{python}
# Learn morea about Code Cells: https://quarto.org/docs/reference/cells/cells-jupyter.html

# Include and execute your code here
df = pl.read_csv("https://posit.byui.edu/content/0d43d66f-d3b7-4c43-8c36-52e3ed69e7f1/names_year_csv.csv")
```

## QUESTION 1

__What was the earliest year that the name ‘Felisha’ was used?__

1964

```{python}
# Q1
df.select(
  "name",
  "year"
).filter(
  name="Felisha"
).sort(
  "year"
).head()

```


## QUESTION 2

__What year had the most babies named 'David'? How many babies were named 'David' that year?__

1955 had the most babies named 'David' with 86,389.

```{python}
# Q2
df.select(
  "name",
  "year",
  "Total"
).filter(
  name="David"
).sort(
  "Total",
  descending=True
).head()

```


## QUESTION 3

__What year did your name hit its peak? How many babies were named your name in that year?__

My name, Tristan, hit its peak in 1996 with 5,810.

```{python}
# Q3
df.select(
  "name",
  "year",
  "Total"
).filter(
  name="Tristan"
).sort(
  "Total",
  descending=True
).head()
```


## QUESTION 4

__In the most recent year, what was the most common female name in Utah?__ 

Olivia

```{python}
# Q5
df.select(
  "name",
  "year",
  "UT"
).filter(
  year=2024
).sort(
  "UT",
  descending=True
).head(10)
```


---
